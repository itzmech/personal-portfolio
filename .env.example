# Contact form (Resend) — optional. The form validates and returns an
# honest "not configured" response until these are set.
# After setting them: npm install resend, then uncomment the marked
# block in app/api/contact/route.ts.
RESEND_API_KEY=
CONTACT_TO_EMAIL=yansh875@gmail.com
